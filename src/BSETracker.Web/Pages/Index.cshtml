@page
@model IndexModel

<!DOCTYPE HTML>
<html>
	<head>
		<meta charset = "utf-8">
		<title>BSE Tracker</title>
		<link rel="stylesheet" href="/style.css">
	</head>
	<body>
        <div id="wrapper">
            @if(Model.Success == true)
            {
                @: Success
            }
            <form method="POST">
                <input asp-for="VM.Email" />
                <input type="submit" />
            </form>
        </div>
	</body>
</html>
