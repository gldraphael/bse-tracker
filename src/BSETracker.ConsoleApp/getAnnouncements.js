module.exports = function (callback, html) {
    let announcements = [];
    // TODO: populate announcements. Refer to scrapexp.js for the time being
    callback(/* error */ null, announcements);
};
